import type { FC } from "react";

const UserJsonPropType={
    item: IUserJson
}

const UserJsonComponent:FC<UserJsonPropType> = () => {
    return (
        <div>

        </div>
    );
};

export default UserJsonComponent;