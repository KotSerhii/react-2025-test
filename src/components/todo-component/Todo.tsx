import type { FC } from "react";
import type { ITodo } from "../../models/ITodo";

type TodoPropType = {
    todo: ITodo;
}

const Todo:FC<TodoPropType> = () => {
    return (
        <div>
            
        </div>
    );
};

export default Todo;